# netlify.toml

# Always revalidate the shell HTML so it references *current* chunk names
[[headers]]
  for = "/index.html"
  [headers.values]
  Cache-Control = "no-cache"

# Keep your SPA redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200